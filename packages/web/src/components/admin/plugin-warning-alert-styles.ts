/**
 * Shared amber "warning" `Alert` styling for the plugin admin banners
 * (`PluginDepsBanner`, `PluginReadinessBanner`) — a single source so
 * their visual treatment can't drift apart.
 */
export const PLUGIN_WARNING_ALERT_CLASS = 'border-amber-500 text-amber-700 dark:text-amber-400 [&>svg]:text-current';
export const PLUGIN_WARNING_ALERT_DESCRIPTION_CLASS = 'text-amber-700/90 dark:text-amber-400/90';
