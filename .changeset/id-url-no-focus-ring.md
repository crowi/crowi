---
"@crowi/web": patch
---

Opening a page by its id URL (`/<page id>`) no longer shows a focus ring around the whole page after it redirects to the page's path. Moving between pages with the keyboard still puts focus, and its ring, on the page content as before.
