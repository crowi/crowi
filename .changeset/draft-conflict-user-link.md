---
'@crowi/web': patch
---

In the "this path is already taken / being created by …" draft conflict alert, the owner's name is now a link to their user page (`/user/<username>`), so you can reach out to them directly. Shared by the `/_edit?path=` create flow and the `/me/creating-pages` new-draft form.
