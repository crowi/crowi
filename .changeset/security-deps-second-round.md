---
'@crowi/api': patch
---

Further security dependency updates, following a second batch of advisories issued against packages fixed hours earlier: `brace-expansion` moves to `1.1.18` / `2.1.4` (the earlier `1.1.17` / `2.1.3` fix was incomplete upstream), `postcss` to `8.5.23`, and `fast-uri` to `3.1.5`.
