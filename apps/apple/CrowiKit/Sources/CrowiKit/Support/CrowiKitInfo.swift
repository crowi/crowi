/// Package-wide constants. `version` is a human-readable marker only (not a
/// semver contract) — Phase 0 scaffold uses it to prove the App target
/// actually links against CrowiKit rather than hardcoding a string.
public enum CrowiKitInfo {
    public static let version = "0.0.1-phase0"
}
