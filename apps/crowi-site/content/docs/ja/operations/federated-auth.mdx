---
title: 外部アカウントでのサインイン
description: Google などの OAuth2 / OIDC プロバイダを使ったサインインの設定
---

Crowi は認証プロバイダをプラグインとして追加できます。`@crowi/plugin-google` を有効にすると、サインイン画面に「Google でサインイン」ボタンが現れ、Google アカウントで Crowi にサインインできるようになります。設計の詳細は [RFC-0014](https://github.com/crowi/crowi/blob/main/docs/rfcs/0014-auth-provider-plugins.md) を参照してください。

Google は特別扱いされていません。プロトコル部分 (OIDC discovery、state / PKCE / nonce、トークン交換、ID トークン検証) はすべて Crowi 本体が持っていて、プラグインが持つのは「どこを discovery するか」「どのスコープを要求するか」「ボタンを何と呼ぶか」の 3 つだけです。同じ仕組みで他の OIDC プロバイダのプラグインも書けます。

## 前提となる環境変数

外部サインインのリダイレクト URI は、**リクエストの `Host` ヘッダからではなく設定された公開 URL から組み立てられます**。したがって次のどちらかが必要です。

| 変数 | 既定 | 用途 |
| ---- | ---- | ---- |
| `AUTH_PUBLIC_WEB_URL` | `CLIENT_URL` | ブラウザが見る Crowi の公開 origin |
| `AUTH_PUBLIC_API_URL` | 解決後の web URL | API の公開 origin。web と同一 origin なら設定不要 |

どちらも origin のみ (パス・クエリ・フラグメントを含まない) である必要があり、含めるとブート時に失敗します。**両方とも未設定の場合、外部サインインは静かに無効化されます** — ブートは成功し、プロバイダ一覧が空になり `/api/auth/providers/<name>/start` が 404 を返します。ボタンが出ないときはまずここを疑ってください。

クライアントシークレットを暗号化して保存するために [設定値の暗号化](./encryption) (`CROWI_ENCRYPTION_KEY`) も強く推奨します。未設定でも動作しますが、シークレットは平文で DB に保存されます。

## セットアップ

### 1. プラグインを有効化する

ランナープロジェクトの `package.json` の依存に `@crowi/plugin-google` を追加し、`crowi.config.json` の `plugins` 配列に列挙します。

```json
{
  "plugins": ["@crowi/plugin-google"]
}
```

### 2. Google Cloud で OAuth クライアントを作る

[Google Cloud Console](https://console.cloud.google.com/apis/credentials) で「OAuth 2.0 クライアント ID」を **ウェブアプリケーション** として作成し、**承認済みのリダイレクト URI** に次を登録します。

```
<AUTH_PUBLIC_API_URL>/api/auth/providers/google/callback
```

たとえば `AUTH_PUBLIC_API_URL="https://wiki.example.com"` なら `https://wiki.example.com/api/auth/providers/google/callback` です。ここが 1 文字でも違うと Google 側で `redirect_uri_mismatch` になります。

### 3. 管理画面に認証情報を入力する

管理画面の **プラグイン** 設定で Google プラグインを開き、Client ID と Client secret を入力して保存します。再起動は不要で、保存した時点でサインイン画面にボタンが現れます。

Client ID と secret は **2 つで 1 つの設定** として単一のドキュメントに保存されます。片方だけが保存された状態は発生せず、片方だけを入力した場合はプロバイダが「未設定」のままになるので、**必ず失敗するボタンがサインイン画面に出ることはありません**。

## サインイン時の挙動

初めて Google でサインインしたユーザーの扱いは、[管理画面](./admin) の **登録モード (registrationMode)** の設定に従います。

| 登録モード | 未登録ユーザーが Google でサインインしたとき |
| ---------- | -------------------------------------------- |
| Open | ユーザー名を選ぶ画面を経て、アカウントが作成され即座に有効になる |
| Restricted | ユーザー名を選ぶ画面を経てアカウントが作成され、**管理者の承認待ち**になる |
| Closed | サインインを拒否し、登録が閉じている旨をサインイン画面に表示する |

いくつかの重要な制約があります。

- **メールアドレスは Google が verified と主張した場合のみ**使われます。未検証のメールアドレスは受け付けません。
- **既存のローカルアカウントと同じメールアドレスでも自動的には紐付きません。** その場合はサインインがエラーになります。パスワードでサインインしてから、設定画面で明示的にアカウントを連携してください。これは、メールアドレスの一致だけで既存アカウントの支配権を渡さないための意図的な設計です。

## アカウントの連携と解除

サインイン済みのユーザーは、**設定 → パスワード/APIトークン/アカウント連携** から、自分のアカウントに外部プロバイダを紐付けたり外したりできます。連携は 1 回の top-level リダイレクトではなく、認証済みの 3 段階で進みます。

1. **連携する** を押すと、認証済みの `POST /auth/providers/<name>/link-start` が呼ばれ、認可 URL と短命でフロー専用の cookie を受け取ります。ブラウザはそのままプロバイダへ遷移します。
2. プロバイダから Crowi のコールバックへ戻ると、Crowi のユーザーデータを一切読み書きせずにフローだけを検証し、1 回限りの確認コードを発行して `/me?provider=<name>&link_completion=<code>` へリダイレクトします。
3. 設定画面には確認ダイアログが表示され (プロバイダの表示名に加え、プロバイダがメールアドレスを返す場合はそれも表示します)、ユーザーがもう一度 **連携する** を押して確認コードを消費する認証済み `POST` を送った時だけ、実際にアカウントが連携されます。

それぞれの段階には別々の有効期限があります。link-start の状態は 300 秒、確認コードはコールバックが発行してから 300 秒、消費済みのコードは 5 分間保持されます。5 分間の保持により、最終 `POST` の応答が失われたブラウザでも同じリクエストを安全に再送でき、同じ結果を受け取れます。

連携は **同一サイトのみ** に対応します。`link-start` のリクエストは `credentials: 'include'` 付きで送られるため、`AUTH_PUBLIC_WEB_URL` と `AUTH_PUBLIC_API_URL` が異なる split-origin 構成でも、両方が同じ registrable domain にある限り動作します — Crowi の本番 CORS allow-list には `CLIENT_URL` に加えて、解決済みの `AUTH_PUBLIC_WEB_URL` オリジンが厳密一致で含まれます。まったく別ドメイン間で third-party cookie が必要になる構成には対応していません。

**マルチインスタンス構成では `REDIS_URL` の設定が必須です。** コールバックを処理した API レプリカが発行した確認コードを、後で確認を処理する別のレプリカからも参照できるよう、共有 Redis が必要になります。この要件は fail-closed です。`CROWI_MULTI_INSTANCE` を宣言しているのに Redis クライアントが利用できない場合、最初の `link-start` リクエストの時点で、cookie・authorization URL・確認コードのいずれも発行せずに `500` を返します。レプリカ間で共有できないストアへ黙ってフォールバックするのではなく、連携機能そのものを利用不可にします。シングルインスタンス構成 (`CROWI_MULTI_INSTANCE` を宣言していない場合) はプロセス内ストアにフォールバックするため、連携の途中で API プロセスが再起動すると、その連携は続行できません。Redis モードでは、上記のどの期限 (link-start の状態、確認コード、消費済みの保持) も API プロセスのローカル時計ではなく Redis 自身の時計 (`TIME`) で判定されるため、システム時計がずれたレプリカがいても、どの期限も短くも長くもなりません。

確認コードはプロバイダからの戻り URL に一時的に含まれるため (`?provider=...&link_completion=...`)、そのリクエストに限りブラウザの履歴、`Referer` ヘッダー、リバースプロキシのアクセスログに残り得ます。確認コード単体では何も連携できません — 消費するには連携を開始したのと同じユーザーの認証済みセッションが必要で、設定画面はコードを読み取った直後に URL から取り除きます。

**アカウントを挿入する直前に**、Crowi はアカウントを改めて読み直し、アクティブなままであること、そして連携開始後にセッション世代 (パスワードリセットや強制サインアウト) が変わっていないことを確認します。どちらかが崩れていれば、何も連携せずにフローを終了し、その確認コードは以後再利用できません。この確認より後には補償処理 (ロールバック) を行いません — 確認してから挿入するまでの短い区間は、2 つの操作がどちらの順序で完了しても構わない、通常のあり得る順序の一つとして扱います。

連携の解除には、**パスワードでサインインできる状態であること**が必要です。次のいずれかに当てはまるときは拒否されます。

- そのユーザーがパスワードを設定していない
- インスタンス全体でパスワードサインインが無効化されている

判定に使われるのはパスワードの有無だけで、**他のプロバイダを連携していても条件を満たしたことにはなりません**。Google と GitHub の 2 つを連携していてパスワードが未設定なら、どちらの解除も拒否されます。連携の数を数える方式にすると、同時に 2 つの解除が走ったときに互いの存在を見て両方が通り、サインイン手段が 1 つも残らないアカウントが生まれうるためです。

### 管理者から見た連携

管理画面のユーザー一覧では、連携しているユーザーの行にアイコンが出ます。行メニューから**管理者が連携を解除**することもできます。ただし**条件は本人が行う場合とは異なります**。本人の解除はパスワード未設定なら拒否されますが、管理者の解除は**対象にパスワードが無ければ発行してから**解除し、発行した平文を一度だけ表示します。乗っ取られたプロバイダアカウントからの復旧が管理者解除の目的だからです。既にパスワードがある場合はそのまま残ります (解除自体は、本人が知っているパスワードを無効化する理由にならないため)。

拒否されるのは **自分自身を解除しようとした場合** と、**インスタンス全体でパスワードサインインが無効な場合** の 2 つで、どちらも `409` を返します。

**連携済みアカウントのメールアドレスは、管理者からも変更できません。** 別のアドレスを指定すると `409 EMAIL_LOCKED_BY_FEDERATED_IDENTITY` で拒否されます。連携済みアカウントは crowi の外からサインインできるため、アドレスを差し替えると確認リンクの宛先 — つまりアカウントの復旧手段 — が持ち主から離れてしまうからです。ユーザー自身の設定画面でも同じ理由で固定されています ([ユーザーページとプロフィール](../guide/profile) 参照)。

## うまくいかないとき

**サインイン画面にボタンが出ない**
`AUTH_PUBLIC_WEB_URL` (または `CLIENT_URL`) が設定されているか、Client ID と secret の**両方**が保存されているかを確認してください。どちらが欠けていても、プロバイダは一覧に現れません。

**Google 側で `redirect_uri_mismatch` になる**
Google Cloud Console に登録したリダイレクト URI と、`AUTH_PUBLIC_API_URL` から組み立てられる URI が完全に一致しているか確認してください。リバースプロキシ配下では、`AUTH_PUBLIC_API_URL` に**プロキシの外側の origin**を設定する必要があります。

**「そのメールアドレスは既に登録されています」と表示される**
そのメールアドレスのローカルアカウントが既に存在します。パスワードでサインインし、設定画面から Google アカウントを連携してください。
