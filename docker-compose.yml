services:
  mongodb:
    image: mongo:8
    volumes:
      - ./data/mongodb:/data/db
    restart: unless-stopped
    ports:
      - 37017:27017

  redis:
    image: redis:7.4
    restart: unless-stopped
    ports:
      - 16379:6379
